/*
 * No type or interface for the moment
 */


/**
 * Concats firstName and lastName
 * @param person 
 * @returns 
 */
export const getFullName = (person) => {
    //Todo
}

/**
 * 
 * @param person Returns tru if age < 18
 * @returns 
 */
export const isMinor = (person) => {
    //Todo
}